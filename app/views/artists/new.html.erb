<h1> Artist Form </h1>

<%= form_for Artist.new do |f| %>
  <%= f.label 'artist_name' %>
  <%= f.text_field :name %>

  <%= f.label 'artist_bio' %>
  <%= f.text_field :bio %>

  <%= f.submit "Create Artist" %>
<% end %>
